services:
  web:
    build:
      context: .
      dockerfile: web/Dockerfile
    image: eztools-web
    container_name: eztoollinux
    ports:
      - "80:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
